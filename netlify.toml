[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--loglevel=error"

# SPA routing for react-router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
